.terraform/
# .terraform.lock.hcl is intentionally NOT ignored -- commit it so every
# developer resolves the same provider version.
# backend.hcl is intentionally NOT ignored -- it's shared, non-secret bucket
# coordinates, not per-developer config. Commit it.
terraform.tfstate
terraform.tfstate.backup
terraform.tfvars
*.tfplan
crash.log
